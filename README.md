
# 🚀 MERN Stack Deployment Guide (Free Tier)

## vercel
# 🏥 Project Deployment Guide

এই ডকুমেন্টটিতে আমাদের ফুল-স্ট্যাক প্রজেক্টটি (Frontend, Backend, Database) কীভাবে Neon PostgreSQL এবং Vercel-এ ডেপ্লয় করা হয়েছে তার বিস্তারিত গাইডলাইন দেওয়া হলো।

---

## 🗄️ ১. ডাটাবেজ সেটআপ (Neon PostgreSQL)

প্রথমে আমাদের ডাটাবেজ লেয়ারটি প্রস্তুত করতে হয়েছে:

1. **Neon.tech**-এ একটি অ্যাকাউন্ট তৈরি করে নতুন প্রজেক্ট ক্রিয়েট করা হয়েছে।
2. রিজিয়ন হিসেবে **Singapore (ap-southeast-1)** সিলেক্ট করা হয়েছে।
3. ড্যাশবোর্ড থেকে **DATABASE_URL** সংগ্রহ করা হয়েছে যা দেখতে অনেকটা এমন:  
   `postgresql://owner:password@ep-winter-moon.aws.neon.tech/neondb?sslmode=require`

---

## ⚙️ ২. ব্যাকএন্ড ডেপ্লয়মেন্ট (Express.js & Prisma)

ব্যাকএন্ড ডেপ্লয় করার জন্য Vercel CLI ব্যবহার করা হয়েছে:

1. **Prisma Generate:** প্রথমে `npx prisma generate` দিয়ে প্রিজমা ক্লায়েন্ট তৈরি করা হয়েছে।
2. **Vercel CLI Run:** ব্যাকএন্ড ফোল্ডারে `vercel` কমান্ডটি চালানো হয়েছে।
3. **Settings:** ডেপ্লয়মেন্টের সময় সব ডিফল্ট সেটিংস রাখা হয়েছে।
4. **Environment Variables:** ভার্সেল ড্যাশবোর্ডে গিয়ে `DATABASE_URL`, `JWT_SECRET`, এবং পেমেন্ট গেটওয়ের (SSLCommerz) ক্রেডেনশিয়ালগুলো যুক্ত করা হয়েছে।
5. **Final Production:** সব সেটিংস শেষে `npx vercel --prod` দিয়ে লাইভ করা হয়েছে।

---

## 💻 ৩. ফ্রন্টএন্ড ডেপ্লয়মেন্ট (Next.js)

Next.js প্রোজেক্টটি **Vercel CLI** দিয়ে হোস্ট করার সময় নিচের প্রশ্নগুলোর উত্তর দেওয়া হয়েছে:

| প্রশ্ন (Vercel CLI Question) | উত্তর (Action/Answer) |
| :--- | :--- |
| **Set up and deploy?** | `Y` (হ্যাঁ) |
| **Which scope do you want to deploy to?** | `Your Vercel Username` (সিলেক্ট করতে হবে) |
| **Link to existing project?** | `N` (নতুন প্রোজেক্ট হিসেবে) |
| **What's your project's name?** | `healthcare-client` (অথবা আপনার দেওয়া নাম) |
| **In which directory is your code located?** | `./` (এন্টার চাপুন) |
| **Want to use the default Deployment Protection settings?** | `Y` (Standard Protection এর জন্য) |
| **Detected a repository. Connect it to this project?** | `Y` (গিটহাবের সাথে কানেক্ট করার জন্য) |
| **Want to modify build settings?** | `N` (ডিফল্ট সেটিংস রাখার জন্য) |

সবশেষে, ভার্সেল থেকে প্রাপ্ত প্রোডাকশন লিঙ্কটি (যেমন: `healthcare-client-theta.vercel.app`) ব্যাকএন্ডের `FRONTEND_URL` হিসেবে সেট করা হয়েছে।

---

## 🛠️ ৪. সমাধানকৃত চ্যালেঞ্জসমূহ (Troubleshooting)

* **EPERM Error (Windows):** লোকাল হোস্টে রান করার সময় `.next` ফোল্ডার ডিলিট করে এবং টার্মিনাল Administrator হিসেবে রান করে সমস্যার সমাধান করা হয়েছে।
* **InvalidTokenError:** ব্রাউজারের LocalStorage ক্লিয়ার করে এবং টোকেন ডিকোডিংয়ের সময় `try-catch` ব্যবহার করে এটি হ্যান্ডেল করা হয়েছে।
* **CORS Issue:** ব্যাকএন্ডে `credentials: true` এবং নির্দিষ্ট `origin` সেট করার মাধ্যমে পেমেন্ট এবং ডেটা ফেচিং নিশ্চিত করা হয়েছে।
* **Cron Job:** সার্ভার পারফরম্যান্স ঠিক রাখতে `node-cron` এর সময় বাড়িয়ে (প্রতি ২ বা ৩ ঘণ্টা) সেট করা হয়েছে।

---

## 🚀 আপডেট করার নিয়ম
পরবর্তীতে কোনো কোড পরিবর্তন করলে শুধু নিচের কমান্ডগুলো দিলেই সাইট অটোমেটিক আপডেট হয়ে যাবে:
```bash
git add .
git commit -m "Your update message"
git push
```
## অথবা সরাসরি CLI দিয়ে:
```bash
npx vercel --prod
```

## Render

এক্ষেত্রে শেখার জন্য অথবা সিভিতে প্রজেক্ট লাইভ সাইট দেখানোর জন্য আমরা Render ব্যবহার করতে পারি। Render লার্নারদের জন্য তাদের ফ্রি সার্ভিস এখনও চালু রেখেছে।

---

## (Table of Contents)
- [ব্যাকেন্ড ডেপ্লয়মেন্ট (Render)](#-ব্যাকেন্ড-ডেপ্লয়মেন্ট-render)
- [সতর্কতা ও ভুল সংশোধন](#-সতর্কতা-ও-ভুল-সংশোধন)
- [ফ্রন্টএন্ড কানেক্টিভিটি](#-ফ্রন্টএন্ড-কানেক্টিভিটি)
- [ফ্রন্টএন্ড ডেপ্লয়মেন্ট (Netlify)](#-ফ্রন্টএন্ড-ডেপ্লয়মেন্ট-netlify)

---

## 🌐 ব্যাকেন্ড ডেপ্লয়মেন্ট (Render)

Render এ হোস্টিং করার জন্য নিচের ধাপগুলো অনুসরণ করুন:

1. প্রথমে আপনার **GitHub** একাউন্ট দিয়ে লগইন করবেন।
2. এরপর **Deploy** অপশন পাবেন (Web Service)।
3. যেই রিপোজিটরি টা Render এ হোস্ট করতে চান, সেই রিপোজিটরি কানেক্ট করতে হবে।
   - *ধরি আপনি আপনার ব্যাকেন্ড সার্ভার হোস্ট করবেন, সেক্ষেত্রে আপনার ব্যাকেন্ড গিটহাব রিপোজিটরি Render এর সাথে কানেক্ট করতে হবে।*
4. কানেক্ট হয়ে গেলে একটা ফর্ম আসবে যেখানে প্রয়োজনীয় ইনফরমেশন এড করতে হবে।

---

## ⚠️ সতর্কতা ও ভুল সংশোধন

ডেপ্লয় করার সময় যেখানে আমরা সাধারণত ভুল করি, সেগুলো দেখে নিন:

### ১. Environment Variables
প্রথমেই আপনার **Environment variables** গুলো ঠিকঠাক দিতে হবে। এক্ষেত্রে আপনার প্রজেক্টের `.env` ফাইলের key এবং value গুলো কপি করে পেস্ট করে দিন।

### ২. সঠিক কমান্ড প্রদান
এই দুটো কমান্ড ঠিকঠাক না দিলে ডেপ্লয় হবে না, failure আসবে:
- **Build Command:** `npm install` (যদি ব্যাকেন্ড node express হয়)
- **Start Command:** `node index.js` (অথবা আপনার মেইন ফাইলের নাম)

সবকিছু ঠিক থাকলে Confirm করে দিলে Render অটো আপনার ব্যাকেন্ড হোস্ট করে আপনার হোস্টিং **API লিঙ্ক** প্রোভাইড করবে।

---

## 🔗 ফ্রন্টএন্ড কানেক্টিভিটি

Render থেকে পাওয়া নতুন API লিঙ্কটি আপনার ফ্রন্টএন্ডে `localhost:5000`-এর পরিবর্তে রিপ্লেস করে দিন। এরপর ফ্রন্টএন্ড Netlify তে হোস্ট করার জন্য প্রস্তুত।

---

## ⚡ ফ্রন্টএন্ড ডেপ্লয়মেন্ট (Netlify)

Netlify তে হোস্ট করার জন্য নিচের নিয়ম অনুসরণ করুন:

1. আপনার VS Code এর terminal ওপেন করুন।
2. এরপর কমান্ড দিন: `npm run build`
3. এই কমান্ডটি দেওয়ার পর আপনার প্রজেক্টে একটি **dist** (অথবা build) ফাইল তৈরি হবে।
4. এবার এই **dist** ফাইলটি আপনার Netlify একাউন্টে গিয়ে New Project তৈরি করে কম্পিউটার থেকে **Drag** করে Netlify তে **Drop** করুন।

---

**কাজ শেষ! অভিনন্দন আপনার প্রজেক্টের জন্য।** 🌻
